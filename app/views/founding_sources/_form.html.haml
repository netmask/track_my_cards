= form_for(founding_source) do |f|
  - if founding_source.errors.any?
    #error_explanation
      %h2
        = pluralize(founding_source.errors.count, "error")
        prohibited this founding_source from being saved:
      %ul
        - founding_source.errors.full_messages.each do |message|
          %li= message
  .actions
    = f.submit
