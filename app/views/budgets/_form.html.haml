= form_for(budget) do |f|
  - if budget.errors.any?
    #error_explanation
      %h2
        = pluralize(budget.errors.count, "error")
        prohibited this budget from being saved:
      %ul
        - budget.errors.full_messages.each do |message|
          %li= message
  .actions
    = f.submit
